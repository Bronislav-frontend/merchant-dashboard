export interface FormValues {
  name: string;
  email: string;
  password: string;
  shop_token: string;
  google_token: string;
  step: number;
}

export interface UserInfo {
  email: string;
  name: string;
  password: string;
}
